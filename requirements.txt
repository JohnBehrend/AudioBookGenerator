# Audiobook Pipeline Dependencies
# Install with: pip install -r requirements.txt

# PyTorch and related libraries
torch>=2.8.0
torchaudio>=2.8.0
torchvision>=0.20.0

# Transformers and tokenizers
transformers>=4.48.0
tokenizers>=0.13

# WhisperX for STT validation
faster-whisper>=1.1.1
whisperx>=3.8.0

# LLM / OpenAI API
openai>=1.0.0

# TTS Engines
# VibeVoice or KugelAudio (local, already in repo)

# Audio processing
pydub
scipy>=1.11.0
numpy>=1.24.0
pandas>=2.0.0

# Gradio for web interface
gradio>=4.0.0

# EPUB parsing
ebooklib>=0.18.0
beautifulsoup4>=4.12.0

# File formats
pillow>=9.0.0

# Utility libraries
tqdm>=4.65.0
requests>=2.31.0
filelock>=3.12.0
fsspec>=2023.5.0
huggingface-hub>=0.21.0

# For audio denoising (fallback included if not available)
# sidon-demo-app (optional, fallback implemented in parse_epub.py)

# Watermarking (optional)
# audioseal (optional, uses fallback if not installed)